---
title: "Auditing Portal overview"
description: "Technical overview of the Arcane backend, PostgreSQL storage, scanner, interpretation worker, API, and Audit UI."
---

The Auditing Portal is the off-chain part of Arcane. It indexes registered contracts, stores raw and interpreted audit data, enforces access, manages disclosure cases, generates reports, and records activity logs.

## Auditing Portal components

```mermaid
flowchart LR
  rpc["Stellar RPC"]
  identity["Identity provider"]

  subgraph backend["NestJS backend"]
    auth["AuthModule\nWorkosAuthModule\nMagicAuthModule"]
    registry["ApplicationsModule\nContractsModule\nAssetsModule"]
    scanner["ScannerModule\nStellarLedgerScannerService"]
    interpreter["AuditInterpretationRunnerService"]
    auditApi["AuditModule"]
    cases["CasesModule\nDisclosureRequestModule"]
    reports["ReportsModule"]
    log["AuditorsLogModule"]
    admins["AdminsModule"]
  end

  subgraph db["PostgreSQL"]
    registryDb["chain, contracts, assets, applications"]
    auditDb["stellar_scans, audit, audit_chain, audit_interpretation"]
    accessDb["organisations, team_member_meta, team_member_application_permissions"]
    workflowDb["disclosure_requests, case_requests, cases, reports, auditors_log"]
  end

  subgraph ui["Audit UI"]
    orgUi["Organization owner workspace"]
    appUi["Application workspace\n/workspace/application/:foreignId"]
  end

  rpc --> scanner
  identity --> auth
  registry --> registryDb
  registryDb --> scanner
  scanner --> auditDb
  auditDb --> interpreter
  interpreter --> auditDb
  auth --> accessDb
  admins --> accessDb
  auditApi --> auditDb
  cases --> workflowDb
  reports --> workflowDb
  log --> workflowDb
  orgUi --> auth
  appUi --> auth
  orgUi --> admins
  appUi --> auditApi
  appUi --> cases
  appUi --> reports
  appUi --> log
```

## Runtime responsibilities

| Area | Implementation | Responsibility |
| --- | --- | --- |
| Backend runtime | NestJS, TypeORM, scheduled jobs | Module graph, API routes, scanner ticks, interpretation batches |
| Registry | `chain`, `contracts`, `assets`, `applications` | Defines which contracts are scanned and how contracts map to organizations/applications |
| Scanner | `ScannerModule`, `StellarLedgerScannerService` | Reads Stellar RPC, parses Soroban events, writes raw `audit` rows and `stellar_scans` checkpoints |
| Interpretation | `AuditInterpretationRunnerService` | Decrypts raw audit rows and writes normalized `audit_interpretation` rows |
| Identity/access | `AuthModule`, `WorkosAuthModule`, `MagicAuthModule`, guards | Maps authenticated users to organizations, applications, permission buckets, and route scope |
| Disclosure workflow | `CasesModule`, `DisclosureRequestModule` | Request creation, approval/close decisions, approved cases, assignments, access windows |
| Reports | `ReportsModule` | Report generation, listing, download, and report metadata |
| Activity log | `AuditorsLogModule` | Activity staging, persistence, list, export |
| UI | React/Vite | Organization owner and application workspaces |

## Documentation

<Columns cols={2}>
  <Card title="Backend Architecture" icon="server" href="/auditing-portal/backend-architecture">
    Backend modules, API groups, scanner and worker runtime.
  </Card>
  <Card title="Data Model" icon="database" href="/auditing-portal/data-model">
    Core PostgreSQL tables and relationships.
  </Card>
  <Card title="Indexing and Interpretation" icon="timeline" href="/auditing-portal/indexing-and-interpretation">
    Stellar RPC scanning, raw audit upsert, interpretation batches, and error states.
  </Card>
  <Card title="Identity and Access" icon="shield" href="/auditing-portal/identity-and-access">
    `GET /auth/me`, permission buckets, workspace resolution, and API enforcement.
  </Card>
  <Card title="Disclosure, Cases, and Reports" icon="file-lines" href="/auditing-portal/disclosure-cases-and-reports">
    Request lifecycle, approved cases, assignments, reports, and activity logs.
  </Card>
  <Card title="Audit UI" icon="desktop" href="/auditing-portal/audit-ui">
    Organization and application workspace routes.
  </Card>
</Columns>
